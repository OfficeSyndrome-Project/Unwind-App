import 'package:flutter/material.dart';

class ScreeningQuestionBoxWidget extends StatelessWidget {
ScreeningQuestionBoxWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}